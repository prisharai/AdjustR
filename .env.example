DATABASE_URL=postgresql://adjustr:adjustr@localhost:5432/adjustr

# AWS S3 (optional — falls back to local ./uploads/ if unset)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
S3_BUCKET_NAME=adjustr-uploads

# Model
LOCATE_ANYTHING_MODEL=nvidia/LocateAnything-3B

DEBUG=true
